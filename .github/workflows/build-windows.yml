name: Build Windows

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive

      - name: Cache Build
        uses: actions/cache@v4
        with:
          path: build
          key: windows-build-${{ hashFiles('CMakeLists.txt') }}
          restore-keys: windows-build-

      - name: Configure
        run: cmake -B build -G "Visual Studio 17 2022" -A x64 -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --config Release --parallel

      - name: Package
        shell: bash
        run: |
          mkdir -p out
          find build -iname "*.exe" -exec cp {} out/ \;
          find build -iname "*.vst3" -exec cp -r {} out/ \;

      - name: Zip
        run: Compress-Archive -Path out\* -DestinationPath Analytiks-Windows.zip

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Analytiks-Windows
          path: Analytiks-Windows.zip
